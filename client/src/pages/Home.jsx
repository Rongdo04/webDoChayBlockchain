// Home.jsx (legacy version) - kept temporarily; user-facing routing now uses user/pages/Home.jsx
// TODO: Remove this duplicate once all references are migrated.
// Deprecated stub (legacy version removed). Not used anywhere.
export default function Home() {
  return null;
}
